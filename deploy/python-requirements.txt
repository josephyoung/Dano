# 使用腾讯云内网 PyPI 镜像（Docker build 不继承宿主机 ~/.pip/pip.conf）
--index-url http://mirrors.tencentyun.com/pypi/simple
--trusted-host mirrors.tencentyun.com

# Runtime dependency for original OA Python Skills (also required by auth tests).
httpx==0.28.1

# ── data-analysis-skill 依赖（2026-09-21 首次加入 / 2026-09-22 还原后重装）──

# 核心数据处理
pandas>=2.0.0
numpy>=1.26.0
openpyxl>=3.1.0

# 统计与机器学习
scipy>=1.13.0
scikit-learn>=1.5.0

# 可视化
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0

# 工具类
tabulate>=0.9.0
