include /etc/nginx/dano/http.conf;

server {
  listen 80;
  server_name _;
  return 308 https://$host:${DANO_HTTPS_PORT}$request_uri;
}

server {
  listen 443 ssl;
  set $dano_oauth_relay_origin "${DANO_OAUTH_RELAY_ORIGIN}";
  include /etc/nginx/dano/tls-server.conf;
}
